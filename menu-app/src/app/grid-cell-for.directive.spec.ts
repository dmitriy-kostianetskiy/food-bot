import { GridCellForDirective } from './grid-cell-for.directive';

describe('GridCellForDirective', () => {
  it('should create an instance', () => {
    const directive = new GridCellForDirective();
    expect(directive).toBeTruthy();
  });
});
